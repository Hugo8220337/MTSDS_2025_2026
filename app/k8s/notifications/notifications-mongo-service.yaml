apiVersion: v1
kind: Service
metadata:
  name: notifications-mongo
  namespace: notifications
spec:
  clusterIP: None
  ports:
    - port: 27017
      targetPort: 27017
  selector:
    app: notifications-mongo
