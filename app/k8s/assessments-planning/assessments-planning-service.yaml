apiVersion: v1
kind: Service
metadata:
  name: assessments-planning
  namespace: assessments-planning
spec:
  selector:
    app: assessments-planning
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080