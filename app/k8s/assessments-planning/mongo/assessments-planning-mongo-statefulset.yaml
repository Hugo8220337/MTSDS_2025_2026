apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: assessments-planning-mongo
  namespace: assessments-planning
spec:
  serviceName: assessments-planning-mongo
  replicas: 1
  selector:
    matchLabels:
      app: assessments-planning-mongo
  template:
    metadata:
      labels:
        app: assessments-planning-mongo
    spec:
      containers:
        - name: mongo
          image: mongo:6.0
          args: ["--noauth"]
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_DATABASE
              value: assessments_content_db
          volumeMounts:
            - name: mongo-data
              mountPath: /data/db
            - name: mongo-init
              mountPath: /docker-entrypoint-initdb.d
              readOnly: true
          readinessProbe:
            exec:
              command:
                - mongosh
                - --quiet
                - --eval
                - 'db.adminCommand({ ping: 1 })'
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 10
          livenessProbe:
            exec:
              command:
                - mongosh
                - --quiet
                - --eval
                - 'db.adminCommand({ ping: 1 })'
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
      volumes:
        - name: mongo-init
          configMap:
            name: assessments-planning-mongo-init
            defaultMode: 0444
  volumeClaimTemplates:
    - metadata:
        name: mongo-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi