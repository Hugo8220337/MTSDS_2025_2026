# Como executar o docker compose de development
Para correr o docker compose de development, basta correr o seguinte comando na pasta /app:
```bash
docker compose -f docker-compose.dev.yaml up --build
```